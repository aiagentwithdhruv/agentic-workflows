# Maps each tool to its required environment variables
# Agent checks this before running a tool to ensure creds are available
# Update this file whenever you add a new tool

tools: {}
  # Example entry:
  # research_web:
  #   requires: [TAVILY_API_KEY]
  #   optional: [EXA_API_KEY]
  # send_email:
  #   requires: [SMTP_HOST, SMTP_USER, SMTP_PASSWORD]
  # scrape_website:
  #   requires: []
  #   notes: "No API key needed — uses requests/beautifulsoup"

# Add tool credentials here as you create tools
